{
  "name": "ip-address-tracker-master",
  "version": "1.0.0",
  "license": "MIT",
  "source": "index.html",
  "scripts": {
    "start": "parcel --open",
    "prebuild": "rm -rf dist",
    "build": "parcel build --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.8.2"
  },
  "dependencies": {
    "leaflet": "^1.9.3"
  }
}
